{
  "buildCommand": "next build",
  "devCommand": "next dev",
  "outputDirectory": ".next",
  "installCommand": "npm install",
  "framework": "nextjs",
  "env": [
    {
      "key": "NEXT_PUBLIC_API_URL",
      "value": "http://localhost:8000",
      "description": "Python backend API URL (update after deployment)"
    }
  ]
}
